@model IEnumerable<GuarderiaAppWeb.Models.Consumo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdMenu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nino)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdMenu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nino.Matricula)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdConsumo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdConsumo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdConsumo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
